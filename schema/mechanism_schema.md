```markdown
# Mechanism Cognition Schema System

## Purpose

This document defines the mechanism cognition schema used by the AI-native presentation cognition system.

The objective is NOT:
scientific complexity accumulation.

The objective IS:
causal scientific interpretability.

Traditional presentation systems often display:

- isolated results
- disconnected figures
- stacked evidence
- fragmented mechanisms

This system focuses on:

- causal understanding
- directional relationships
- mechanism storytelling
- interaction interpretation
- process readability

Mechanism schemas should help audiences understand:

```text
why something happens
```

NOT only:

```text
what happened
```

---

# Core Mechanism Philosophy

Scientific presentations should communicate:

causal logic,
NOT result accumulation.

A mechanism is:

a structured explanatory pathway.

Mechanism communication should guide:

```text
Trigger
↓
Environmental Response
↓
Biological / Physical Interaction
↓
Functional Change
↓
Observable Outcome
```

The system must transform:

scientific complexity
into
interpretable mechanism cognition.

---

# Global Mechanism Structure

Every mechanism schema should regulate:

```text
Cause
↓
Response
↓
Interaction
↓
Function
↓
Outcome
↓
Interpretation
```

Mechanism structures should prioritize:

- directional clarity
- interaction readability
- process isolation
- interpretation stability

---

# Core Mechanism Schema

Every mechanism schema should contain:

```yaml
mechanism_type:
mechanism_goal:
trigger:
environmental_response:
interaction_layer:
functional_change:
observable_outcome:
supporting_evidence:
mechanism_takeaway:
visual_direction:
annotation_behavior:
interpretation_priority:
```

The schema should encode:

scientific reasoning,
NOT figure stacking.

---

# Stage 1 — Mechanism Type

## mechanism_type

Purpose:

define the scientific mechanism category.

Possible values:

- rhizosphere_process
- water_transport
- nitrogen_cycle
- stress_response
- ecological_interaction
- spatial_response
- digital_simulation
- crop_growth_process

Example:

```yaml
mechanism_type:
  nitrogen_cycle
```

Mechanism type regulates:

visual explanation behavior.

---

# Stage 2 — Mechanism Goal

## mechanism_goal

Purpose:

define the scientific explanation objective.

Example:

```yaml
mechanism_goal:
  explain_how_membrane_irrigation_improves_nitrogen_transformation_efficiency
```

The goal should explain:

causal interpretation,
NOT experimental procedure.

---

# Stage 3 — Trigger Layer

## trigger

Purpose:

define the initiating mechanism factor.

Example:

```yaml
trigger:
  membrane_controlled_irrigation
```

Possible triggers:

- irrigation
- salinity
- drought
- microbial_shift
- temperature_change
- fertilization
- environmental_disturbance

Triggers initiate:

mechanism flow.

---

# Stage 4 — Environmental Response Layer

## environmental_response

Purpose:

define environmental or physical changes.

Example:

```yaml
environmental_response:

  - rhizosphere_moisture_stabilization
  - soil_temperature_buffering
  - oxygen_distribution_optimization
```

Responses regulate:

interaction environments.

---

# Stage 5 — Interaction Layer

## interaction_layer

Purpose:

define biological,
chemical,
or physical interactions.

Example:

```yaml
interaction_layer:

  - microbial_activity_enhancement
  - root_microbe_interaction
  - nitrogen_conversion_acceleration
```

Interactions explain:

mechanism connectivity.

---

# Stage 6 — Functional Change Layer

## functional_change

Purpose:

define functional system responses.

Example:

```yaml
functional_change:

  - nitrogen_use_efficiency_improvement
  - nutrient_absorption_enhancement
  - stress_reduction
```

Functional changes connect:

mechanisms to outcomes.

---

# Stage 7 — Observable Outcome Layer

## observable_outcome

Purpose:

define measurable results.

Example:

```yaml
observable_outcome:

  - crop_growth_improvement
  - biomass_increase
  - yield_enhancement
```

Outcomes should remain:

subordinate to mechanism explanation.

---

# Stage 8 — Supporting Evidence

## supporting_evidence

Purpose:

define evidence support systems.

Example:

```yaml
supporting_evidence:

  - microbial_network_analysis
  - nitrogen_cycle_gene_expression
  - soil_moisture_data
  - yield_validation
```

Evidence should support:

causal understanding.

The system must avoid:

evidence overload.

---

# Stage 9 — Mechanism Takeaway

## mechanism_takeaway

Purpose:

define the dominant scientific insight.

Example:

```yaml
mechanism_takeaway:
  stable_rhizosphere_microenvironments_enhance_nitrogen_transformation_efficiency
```

Every mechanism slide should preserve:

one dominant scientific takeaway.

---

# Stage 10 — Visual Direction

## visual_direction

Purpose:

define mechanism reading direction.

Possible values:

- top_to_bottom
- left_to_right
- center_expansion
- radial_interaction
- layered_progression

Example:

```yaml
visual_direction:
  top_to_bottom
```

Mechanism direction should feel:

inevitable and readable.

---

# Stage 11 — Annotation Behavior

## annotation_behavior

Purpose:

define annotation explanation strategy.

Possible values:

- restrained
- mechanism_guided
- teaching_supportive
- evidence_assistive

Example:

```yaml
annotation_behavior:
  mechanism_guided
```

Annotations should clarify:

causal relationships,
NOT overload figures.

---

# Stage 12 — Interpretation Priority

## interpretation_priority

Purpose:

define explanation emphasis.

Possible values:

- mechanism_first
- evidence_balanced
- teaching_accessibility
- interpretation_dominant

Example:

```yaml
interpretation_priority:
  mechanism_first
```

Mechanism interpretation should dominate:

scientific communication.

---

# Full Mechanism Schema Example

```yaml
mechanism_type:
  nitrogen_cycle

mechanism_goal:
  explain_how_membrane_irrigation_improves_nitrogen_transformation_efficiency

trigger:
  membrane_controlled_irrigation

environmental_response:

  - rhizosphere_moisture_stabilization
  - oxygen_distribution_optimization

interaction_layer:

  - microbial_activity_enhancement
  - nitrogen_conversion_acceleration

functional_change:

  - nitrogen_use_efficiency_improvement
  - nutrient_absorption_enhancement

observable_outcome:

  - crop_growth_improvement
  - yield_enhancement

supporting_evidence:

  - microbial_network_analysis
  - nitrogen_cycle_gene_expression
  - yield_validation

mechanism_takeaway:
  stable_rhizosphere_microenvironments_enhance_nitrogen_transformation_efficiency

visual_direction:
  top_to_bottom

annotation_behavior:
  mechanism_guided

interpretation_priority:
  mechanism_first
```

---

# Mechanism Execution Rules

The rendering system must ensure:

- directional readability
- mechanism isolation
- causal clarity
- restrained evidence behavior
- interaction visibility
- hierarchy stability

Mechanism visuals should support:

scientific understanding.

---

# Anti-AI-Slop Mechanism Rules

The system must actively prevent:

- disconnected scientific figures
- infographic-style mechanisms
- decorative arrows
- evidence stacking
- chaotic interaction layouts
- excessive annotation noise

Mechanism slides should feel:

editorially scientific,
human-designed,
and causally interpretable.

---

# Final Validation

Before mechanism execution:

Check:

- Is the trigger obvious?
- Is causal flow understandable?
- Are interactions readable?
- Is evidence subordinate to mechanism explanation?
- Is the mechanism takeaway memorable?
- Does the slide explain why rather than only what?

If NO:

refine mechanism cognition before rendering.

---

# System Identity

This mechanism system is NOT:

scientific figure organization.

It IS:

causal scientific cognition orchestration.

---

# Final Philosophy

Mechanism communication is NOT:
result stacking.

It IS:

causal understanding engineering
for scientific interpretation systems.
```
