```markdown
# Cognition Rendering Contract System

## Purpose

This document defines the rendering contract between the cognition system and the rendering engine.

The objective is NOT:
free-form visual generation.

The objective IS:
cognition-constrained rendering execution.

Traditional rendering systems operate as:

```text
layout generation
→
visual output
```

This system operates as:

```text
cognition planning
↓
schema interpretation
↓
rendering execution
↓
communication validation
```

The renderer must obey:

communication cognition.

Rendering exists to support:

- audience understanding
- hierarchy clarity
- mechanism readability
- narrative progression
- semantic consistency

The renderer must NEVER dominate:

communication reasoning.

---

# Core Rendering Philosophy

Rendering is NOT:

creative decoration.

Rendering IS:

communication execution.

The renderer should behave as:

an editorial execution system,
NOT a graphic experimentation engine.

The renderer must preserve:

- cognition hierarchy
- semantic meaning
- audience adaptation
- presentation identity
- communication stability

---

# Global Rendering Contract

The rendering engine must preserve:

```text
Communication Goal
↓
Hierarchy Structure
↓
Audience Cognition
↓
Narrative Progression
↓
Mechanism Readability
↓
Semantic Consistency
↓
Visual Stability
↓
Rendering Execution
```

Rendering must remain:

subordinate to cognition.

---

# Core Rendering Contract Schema

Every rendering contract should contain:

```yaml
rendering_identity:
hierarchy_contract:
spacing_contract:
semantic_contract:
mechanism_contract:
narrative_contract:
density_contract:
annotation_contract:
visual_stability_contract:
audience_contract:
anti_slop_contract:
evaluation_contract:
```

The rendering contract regulates:

renderer behavior boundaries.

---

# Stage 1 — Rendering Identity

## rendering_identity

Purpose:

define renderer execution philosophy.

Example:

```yaml
rendering_identity:
  cognition_first_rendering
```

The renderer must behave as:

a cognition execution system.

---

# Stage 2 — Hierarchy Contract

## hierarchy_contract

Purpose:

enforce hierarchy preservation.

Example:

```yaml
hierarchy_contract:

  preserve:
    - dominant_focus
    - reading_order
    - visual_priority

  prevent:
    - equal_weight_layouts
    - focal_competition
```

The renderer must preserve:

attention guidance.

---

# Stage 3 — Spacing Contract

## spacing_contract

Purpose:

enforce cognitive spacing behavior.

Example:

```yaml
spacing_contract:

  spacing_behavior:
    relaxed_editorial

  whitespace_priority:
    high

  prevent:
    - compression
    - visual_crowding
```

Spacing should support:

cognitive breathing rhythm.

---

# Stage 4 — Semantic Contract

## semantic_contract

Purpose:

preserve semantic visual meaning.

Example:

```yaml
semantic_contract:

  preserve:
    blue:
      science
      GIS

    green:
      ecology
      agriculture

    orange:
      stress
      emphasis

  prevent:
    - arbitrary_colors
    - decorative_gradients
```

Semantic consistency improves:

interpretation speed.

---

# Stage 5 — Mechanism Contract

## mechanism_contract

Purpose:

preserve causal readability.

Example:

```yaml
mechanism_contract:

  preserve:
    - directional_flow
    - interaction_visibility
    - causal_hierarchy

  prevent:
    - disconnected_mechanisms
    - decorative_arrows
    - chaotic_layouts
```

Mechanism rendering should prioritize:

scientific interpretability.

---

# Stage 6 — Narrative Contract

## narrative_contract

Purpose:

preserve narrative continuity.

Example:

```yaml
narrative_contract:

  preserve:
    - progressive_understanding
    - transition_stability
    - memory_reinforcement

  prevent:
    - abrupt_shifts
    - fragmented_sequences
```

Rendering must support:

narrative cognition.

---

# Stage 7 — Density Contract

## density_contract

Purpose:

control cognitive load.

Example:

```yaml
density_contract:

  target_density:
    medium

  prevent:
    - evidence_overload
    - annotation_noise
    - excessive_simultaneous_focus
```

The renderer should preserve:

interpretation comfort.

---

# Stage 8 — Annotation Contract

## annotation_contract

Purpose:

regulate annotation behavior.

Example:

```yaml
annotation_contract:

  annotation_behavior:
    restrained

  preserve:
    - explanatory_support
    - directional_guidance

  prevent:
    - paragraph_annotations
    - annotation_competition
```

Annotations should support:

understanding,
NOT clutter.

---

# Stage 9 — Visual Stability Contract

## visual_stability_contract

Purpose:

preserve presentation identity.

Example:

```yaml
visual_stability_contract:

  preserve:
    - typography_system
    - spacing_rhythm
    - editorial_tone
    - semantic_behavior

  prevent:
    - visual_personality_drift
    - random_redesign
```

Visual consistency improves:

cognitive continuity.

---

# Stage 10 — Audience Contract

## audience_contract

Purpose:

preserve audience adaptation.

Example:

```yaml
audience_contract:

  audience:
    researchers_and_graduate_students

  preserve:
    - scientific_readability
    - restrained_complexity

  prevent:
    - oversimplification
    - inaccessible_density
```

Audience cognition regulates:

rendering behavior.

---

# Stage 11 — Anti-Slop Contract

## anti_slop_contract

Purpose:

actively prevent AI-generated visual artifacts.

Example:

```yaml
anti_slop_contract:

  prevent:

    - startup_pitch_design
    - infographic_contamination
    - glowing_AI_visuals
    - SaaS_dashboard_layouts
    - decorative_gradient_abuse
    - excessive_icon_usage
    - equal_weight_visuals
```

Slides should feel:

human-designed,
editorially stable,
and cognitively intentional.

---

# Stage 12 — Evaluation Contract

## evaluation_contract

Purpose:

validate rendering cognition quality.

Example:

```yaml
evaluation_contract:

  validate:

    - hierarchy_clarity
    - mechanism_readability
    - narrative_continuity
    - semantic_consistency
    - density_control
    - audience_alignment
    - presentation_identity_preservation
```

Rendering quality should be evaluated through:

communication effectiveness.

---

# Full Rendering Contract Example

```yaml
rendering_identity:
  cognition_first_rendering

hierarchy_contract:

  preserve:
    - dominant_focus
    - reading_order
    - visual_priority

  prevent:
    - equal_weight_layouts
    - focal_competition

spacing_contract:

  spacing_behavior:
    relaxed_editorial

  whitespace_priority:
    high

  prevent:
    - visual_compression
    - layout_crowding

semantic_contract:

  preserve:

    blue:
      science
      GIS

    green:
      ecology
      agriculture

    orange:
      stress
      emphasis

  prevent:
    - arbitrary_colors
    - decorative_gradients

mechanism_contract:

  preserve:
    - directional_flow
    - causal_hierarchy
    - interaction_visibility

  prevent:
    - disconnected_mechanisms
    - decorative_arrows

narrative_contract:

  preserve:
    - progressive_understanding
    - transition_stability
    - memory_reinforcement

  prevent:
    - fragmented_sequences

density_contract:

  target_density:
    medium

  prevent:
    - annotation_overload
    - evidence_competition

annotation_contract:

  annotation_behavior:
    restrained

  preserve:
    - explanatory_support

  prevent:
    - annotation_noise

visual_stability_contract:

  preserve:
    - typography_system
    - spacing_rhythm
    - editorial_tone

  prevent:
    - visual_personality_drift

audience_contract:

  audience:
    researchers_and_graduate_students

  preserve:
    - scientific_readability

  prevent:
    - inaccessible_density

anti_slop_contract:

  prevent:
    - startup_pitch_design
    - infographic_contamination
    - glowing_AI_visuals
    - SaaS_dashboard_layouts

evaluation_contract:

  validate:
    - hierarchy_clarity
    - mechanism_readability
    - semantic_consistency
    - narrative_continuity
    - audience_alignment
```

---

# Rendering Execution Rules

The rendering engine must:

- obey cognition schemas
- preserve hierarchy
- preserve narrative progression
- maintain semantic consistency
- regulate density
- stabilize presentation identity

Rendering should NEVER:

override cognition planning.

---

# Final Validation

Before rendering output:

Check:

- Is hierarchy preserved?
- Is narrative progression stable?
- Is semantic meaning consistent?
- Is density controlled?
- Is the slide readable?
- Does rendering support audience understanding?
- Does the presentation feel human-designed?

If NO:

revise rendering behavior before execution.

---

# System Identity

This rendering contract is NOT:

layout freedom.

It IS:

cognition-constrained rendering execution.

---

# Final Philosophy

Rendering is NOT:
visual creativity.

It IS:

communication cognition execution
for audience understanding systems.
```
