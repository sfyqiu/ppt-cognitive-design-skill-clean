```markdown
# Semantic Visual Schema System

## Purpose

This document defines the semantic visual schema used by the AI-native presentation cognition system.

The objective is NOT:
decorative color styling.

The objective IS:
meaning-oriented visual communication.

Traditional presentation systems treat visuals as:

- decoration
- branding
- beautification
- stylistic enhancement

This system treats visuals as:

- semantic cognition signals
- communication accelerators
- interpretation guidance systems
- audience understanding support

Visual semantics should improve:

understanding speed,
NOT aesthetic complexity.

---

# Core Semantic Philosophy

Visual language should communicate:

meaning before decoration.

Every visual decision should answer:

```text
What does this help the audience understand?
```

The system must avoid:

- arbitrary colors
- decorative gradients
- visual trend imitation
- aesthetic overload

Semantic visuals should feel:

editorial,
restrained,
and cognitively intentional.

---

# Global Semantic Structure

Every semantic visual system should regulate:

```text
Meaning
↓
Association
↓
Interpretation
↓
Attention
↓
Memory
```

Visual semantics should reduce:

interpretation burden.

---

# Core Semantic Schema

Every semantic schema should contain:

```yaml
semantic_system:
color_behavior:
shape_behavior:
line_behavior:
icon_behavior:
contrast_behavior:
emphasis_behavior:
background_behavior:
visual_tone:
```

The schema should encode:

communication meaning,
NOT decoration.

---

# Stage 1 — Semantic System

## semantic_system

Purpose:

define semantic meaning associations.

Example:

```yaml
semantic_system:

  blue:
    science
    GIS
    water
    technology

  green:
    ecology
    agriculture
    vegetation
    sustainability

  orange:
    stress
    warning
    emphasis

  red:
    anomalies
    risks
    degradation

  purple:
    innovation
    advanced_methods
```

Semantic meaning should remain:

stable across slides.

---

# Stage 2 — Color Behavior

## color_behavior

Purpose:

define color usage logic.

Possible values:

- restrained_editorial
- teaching_guided
- institutional_balanced
- scientific_minimal

Example:

```yaml
color_behavior:
  restrained_editorial
```

The system should avoid:

- rainbow palettes
- neon colors
- decorative gradients
- excessive saturation

Colors should communicate:

semantic importance.

---

# Stage 3 — Shape Behavior

## shape_behavior

Purpose:

define semantic shape usage.

Possible values:

- rounded_scientific
- structured_editorial
- minimal_teaching
- restrained_institutional

Example:

```yaml
shape_behavior:
  structured_editorial
```

Shapes should support:

interpretation clarity.

The system must avoid:

decorative geometry overload.

---

# Stage 4 — Line Behavior

## line_behavior

Purpose:

define directional and structural behavior.

Possible values:

- causal_directional
- restrained_connective
- teaching_guided
- minimal_structural

Example:

```yaml
line_behavior:
  causal_directional
```

Lines should communicate:

relationships,
NOT decoration.

---

# Stage 5 — Icon Behavior

## icon_behavior

Purpose:

define icon usage strategy.

Possible values:

- minimal
- semantic_supportive
- teaching_assistive
- institutional_restrained

Example:

```yaml
icon_behavior:
  semantic_supportive
```

Icons should:

support communication,
NOT dominate slides.

The system must avoid:

startup icon overload.

---

# Stage 6 — Contrast Behavior

## contrast_behavior

Purpose:

define hierarchy contrast behavior.

Possible values:

- editorial_soft
- scientific_balanced
- teaching_clear
- institutional_calm

Example:

```yaml
contrast_behavior:
  editorial_soft
```

Contrast should regulate:

attention priority.

Avoid:

aggressive visual competition.

---

# Stage 7 — Emphasis Behavior

## emphasis_behavior

Purpose:

define how emphasis appears.

Possible values:

- semantic_highlight
- restrained_focus
- teaching_guidance
- scientific_emphasis

Example:

```yaml
emphasis_behavior:
  restrained_focus
```

Emphasis should feel:

intentional,
NOT visually loud.

---

# Stage 8 — Background Behavior

## background_behavior

Purpose:

define environmental visual tone.

Possible values:

- clean_editorial
- scientific_light
- teaching_bright
- institutional_neutral

Example:

```yaml
background_behavior:
  clean_editorial
```

Backgrounds should support:

foreground cognition.

The system must avoid:

decorative texture overload.

---

# Stage 9 — Visual Tone

## visual_tone

Purpose:

define overall visual atmosphere.

Possible values:

- editorial_scientific
- calm_academic
- approachable_teaching
- restrained_professional

Example:

```yaml
visual_tone:
  editorial_scientific
```

Visual tone regulates:

presentation personality.

---

# Full Semantic Visual Schema Example

```yaml
semantic_system:

  blue:
    science
    GIS
    water
    technology

  green:
    ecology
    agriculture
    vegetation

  orange:
    stress
    emphasis

  red:
    degradation
    anomalies

color_behavior:
  restrained_editorial

shape_behavior:
  structured_editorial

line_behavior:
  causal_directional

icon_behavior:
  semantic_supportive

contrast_behavior:
  editorial_soft

emphasis_behavior:
  restrained_focus

background_behavior:
  clean_editorial

visual_tone:
  editorial_scientific
```

---

# Semantic Visual Execution Rules

The rendering system must ensure:

- semantic consistency
- restrained visual behavior
- stable editorial atmosphere
- hierarchy-oriented contrast
- cognition-first visuals

Visual systems should accelerate:

understanding.

---

# Anti-AI-Slop Semantic Rules

The semantic system must actively prevent:

- startup visual aesthetics
- infographic contamination
- neon AI gradients
- glowing visual effects
- decorative icon overload
- SaaS dashboard styling

Visual behavior should feel:

human-designed,
editorially grounded,
and academically restrained.

---

# Final Validation

Before semantic execution:

Check:

- Do colors communicate meaning?
- Is visual emphasis intentional?
- Is the atmosphere stable?
- Is visual noise minimized?
- Does semantic behavior support audience understanding?
- Does the visual tone preserve presentation identity?

If NO:

refine semantic cognition before rendering.

---

# System Identity

This semantic system is NOT:

visual styling.

It IS:

meaning-oriented communication guidance.

---

# Final Philosophy

Visual semantics are NOT:
decoration systems.

They ARE:

communication acceleration systems
for audience cognition engineering.
```
