```markdown
# Hierarchy Cognition Schema System

## Purpose

This document defines the hierarchy cognition schema used by the AI-native presentation cognition system.

The objective is NOT:
visual stacking.

The objective IS:
attention orchestration.

Traditional presentation systems treat hierarchy as:

- font scaling
- layout composition
- visual decoration

This system treats hierarchy as:

- audience cognition control
- reading guidance
- communication prioritization
- interpretation sequencing

Hierarchy determines:

what audiences notice,
understand,
and remember first.

---

# Core Hierarchy Philosophy

A presentation is NOT:

a collection of equal visual objects.

A presentation IS:

a controlled cognitive pathway.

Hierarchy exists to regulate:

```text
attention
↓
interpretation
↓
understanding
↓
memory
```

The system must guide audiences intentionally.

---

# Global Hierarchy Structure

Every slide should establish:

```text
Primary Focus
↓
Secondary Support
↓
Contextual Explanation
↓
Takeaway Reinforcement
```

Hierarchy must determine:

- visual dominance
- reading order
- cognitive emphasis
- interpretation rhythm

The system must avoid:

equal-weight communication.

---

# Core Hierarchy Schema

Every hierarchy schema should contain:

```yaml
hierarchy:

  primary:
  secondary:
  tertiary:

dominant_focus:
reading_priority:
visual_weight:
annotation_behavior:
evidence_behavior:
spacing_behavior:
takeaway_priority:
```

The schema should encode:

attention logic,
NOT object geometry.

---

# Stage 1 — Primary Hierarchy

## primary

Purpose:

define the dominant communication focus.

Possible values:

- mechanism_chain
- conceptual_diagram
- comparison_framework
- institutional_positioning
- evidence_cluster
- teaching_visual

Example:

```yaml
primary:
  mechanism_chain
```

The primary layer should:

- dominate attention
- anchor interpretation
- guide reading direction

The audience should identify it within:

3 seconds.

---

# Stage 2 — Secondary Hierarchy

## secondary

Purpose:

support primary interpretation.

Possible values:

- supporting_evidence
- annotations
- process_labels
- comparison_notes
- contextual_figures

Example:

```yaml
secondary:
  supporting_evidence
```

Secondary layers should:

support,
NOT compete.

---

# Stage 3 — Tertiary Hierarchy

## tertiary

Purpose:

provide contextual clarification.

Possible values:

- citations
- metadata
- minor annotations
- supplementary notes

Example:

```yaml
tertiary:
  annotations
```

Tertiary layers should remain:

visually restrained.

---

# Stage 4 — Dominant Focus

## dominant_focus

Purpose:

define the visual anchor.

Possible values:

- center
- top_center
- left_primary
- right_primary
- radial_center

Example:

```yaml
dominant_focus:
  center
```

The dominant focus determines:

eye stabilization.

---

# Stage 5 — Reading Priority

## reading_priority

Purpose:

define interpretation sequencing.

Possible values:

- top_to_bottom
- left_to_right
- center_outward
- progressive_steps
- comparison_scan

Example:

```yaml
reading_priority:
  top_to_bottom
```

Rendering must preserve:

reading cognition.

---

# Stage 6 — Visual Weight

## visual_weight

Purpose:

define hierarchy emphasis strength.

Example:

```yaml
visual_weight:

  primary:
    dominant

  secondary:
    restrained

  tertiary:
    minimal
```

Visual weight should regulate:

- scale
- contrast
- spacing
- emphasis

The system must avoid:

equal emphasis distribution.

---

# Stage 7 — Annotation Behavior

## annotation_behavior

Purpose:

define annotation hierarchy behavior.

Possible values:

- restrained
- explanatory
- progressive
- teaching_oriented

Example:

```yaml
annotation_behavior:
  restrained
```

Annotations should:

support cognition,
NOT overload slides.

---

# Stage 8 — Evidence Behavior

## evidence_behavior

Purpose:

define evidence hierarchy behavior.

Possible values:

- supportive_not_competitive
- dominant_validation
- grouped_support
- minimal_confirmation

Example:

```yaml
evidence_behavior:
  supportive_not_competitive
```

Evidence should support:

communication goals.

---

# Stage 9 — Spacing Behavior

## spacing_behavior

Purpose:

define whitespace hierarchy behavior.

Possible values:

- relaxed_editorial
- compact_scientific
- teaching_spacious
- institutional_balanced

Example:

```yaml
spacing_behavior:
  relaxed_editorial
```

Whitespace regulates:

cognitive breathing rhythm.

---

# Stage 10 — Takeaway Priority

## takeaway_priority

Purpose:

define memory reinforcement strength.

Possible values:

- dominant
- reinforced
- subtle

Example:

```yaml
takeaway_priority:
  reinforced
```

Every slide must preserve:

one memorable takeaway.

---

# Full Hierarchy Schema Example

```yaml
hierarchy:

  primary:
    mechanism_chain

  secondary:
    supporting_evidence

  tertiary:
    annotations

dominant_focus:
  center

reading_priority:
  top_to_bottom

visual_weight:

  primary:
    dominant

  secondary:
    restrained

  tertiary:
    minimal

annotation_behavior:
  restrained

evidence_behavior:
  supportive_not_competitive

spacing_behavior:
  relaxed_editorial

takeaway_priority:
  reinforced
```

---

# Anti-AI-Slop Hierarchy Rules

The hierarchy system must actively prevent:

- equal-weight layouts
- infographic chaos
- dashboard fragmentation
- decorative emphasis competition
- startup visual aggression

The hierarchy should feel:

editorial,
calm,
and intentional.

---

# Final Validation

Before hierarchy execution:

Check:

- Is there one dominant focus?
- Is reading order obvious?
- Is evidence subordinate to communication goals?
- Is annotation density controlled?
- Does whitespace support cognition?
- Is audience attention guided intentionally?

If NO:

refine hierarchy cognition before rendering.

---

# System Identity

This hierarchy system is NOT:

visual stacking logic.

It IS:

audience attention orchestration.

---

# Final Philosophy

Hierarchy is NOT:
visual decoration.

It IS:

controlled cognitive prioritization
for communication understanding engineering.
```
