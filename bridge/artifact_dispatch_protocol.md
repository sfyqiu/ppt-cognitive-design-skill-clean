# Artifact Dispatch Protocol

## Purpose

This document defines how cognition artifacts are generated, dispatched, coordinated, and updated during runtime execution.

The objective is NOT:
document generation sequencing.

The objective IS:
runtime cognition infrastructure orchestration.

Artifacts are NOT:

outputs.

Artifacts ARE:

persistent cognition execution structures.

---

# Core Artifact Dispatch Philosophy

Artifacts MUST emerge through:

cognition progression.

Artifacts MUST NOT be:

randomly generated documents.

Every artifact dispatch must preserve:

- workflow continuity
- communication intent
- hierarchy stability
- semantic consistency
- refinement continuity

Artifacts form:

the cognition runtime backbone.

---

# Global Artifact Dispatch Flow

```text
Intent Interpretation
↓
Cognition Plan Dispatch
↓
Slide Blueprint Dispatch
↓
Rendering JSON Dispatch
↓
Adaptive Layout Dispatch
↓
Renderer Coordination
↓
Diagnostics Dispatch
↓
Refinement Patch Dispatch
↓
Cognition Diff Dispatch
↓
Refinement Trace Dispatch
```

Artifacts evolve through:

runtime cognition flow.

---

# Protocol 1 — Cognition Plan Dispatch

## Requirement

The runtime MUST dispatch:

cognition_plan first.

---

## Mandatory Purpose

The cognition plan defines:

```yaml
define:
  - communication_goal
  - workflow_identity
  - hierarchy_strategy
  - narrative_structure
  - audience_alignment
```

No downstream artifact may exist before:

cognition planning.

---

# Protocol 2 — Slide Blueprint Dispatch

## Requirement

The runtime MUST dispatch:

slide_blueprint after cognition planning.

---

## Mandatory Purpose

The slide blueprint defines:

```yaml
define:
  - slide_goal
  - hierarchy_behavior
  - reading_flow
  - density_behavior
```

Blueprints stabilize:

slide cognition execution.

---

# Protocol 3 — Rendering JSON Dispatch

## Requirement

The runtime MUST dispatch:

rendering_json after slide blueprints.

---

## Mandatory Purpose

Rendering JSON defines:

```yaml
define:
  - hierarchy_encoding
  - semantic_encoding
  - mechanism_encoding
  - adaptive_rendering_behavior
```

Rendering JSON becomes:

executable cognition structure.

---

# Protocol 4 — Adaptive Layout Dispatch

## Requirement

The runtime MUST dispatch:

adaptive_layout_spec before rendering.

---

## Mandatory Purpose

Adaptive layouts define:

```yaml
define:
  - spacing_behavior
  - focus_distribution
  - audience_accessibility
  - cognitive_density
```

Layouts remain:

cognition-aware.

---

# Protocol 5 — Diagnostics Dispatch

## Requirement

The runtime MUST dispatch:

rendering_diagnostics after evaluation.

---

## Mandatory Purpose

Diagnostics define:

```yaml
detect:
  - hierarchy_failures
  - density_failures
  - semantic_instability
  - audience_mismatch
```

Diagnostics precede:

refinement.

---

# Protocol 6 — Refinement Patch Dispatch

## Requirement

The runtime MUST dispatch:

refinement_patch after diagnostics.

---

## Mandatory Purpose

Refinement patches define:

```yaml
refine:
  localized_components

preserve:
  - workflow_identity
  - presentation_identity
```

Refinement remains:

precision-oriented.

---

# Protocol 7 — Cognition Diff Dispatch

## Requirement

The runtime MUST dispatch:

cognition_diff after refinement.

---

## Mandatory Purpose

Cognition diffs explain:

```yaml
explain:
  - what_changed
  - why_changed
  - what_remained_stable
```

Diffs preserve:

refinement explainability.

---

# Protocol 8 — Refinement Trace Dispatch

## Requirement

The runtime MUST dispatch:

refinement_trace continuously.

---

## Mandatory Purpose

Refinement traces preserve:

```yaml
preserve:
  - collaboration_history
  - workflow_continuity
  - refinement_memory
```

Traces enable:

persistent collaboration.

---

# Artifact Dispatch Rules

Artifacts MUST:

- follow cognition progression
- preserve workflow continuity
- preserve semantic stability
- preserve audience alignment
- support diagnostics
- support refinement
- support persistent collaboration

Artifacts MUST NOT:

- generate independently
- reset cognition state
- fragment workflows
- behave statelessly

---

# Anti-AI-Slop Artifact Dispatch Protection

The runtime MUST prevent:

```yaml
prevent:
  - artifact_fragmentation
  - disconnected_generation
  - stateless_artifacts
  - template_artifact_behavior
  - regeneration_loops
```

Artifact dispatch should feel:

structured,
editorial,
and cognition-aware.

---

# Dispatch Validation

```yaml
validate:
  - cognition_plan_exists
  - workflow_identity_preserved
  - hierarchy_behavior_preserved
  - semantic_consistency_preserved
  - refinement_explainability_preserved
```

If NO:

artifact dispatch is invalid.

---

# System Identity

This artifact dispatch protocol is NOT:

document sequencing logic.

It IS:

cognition infrastructure orchestration.

---

# Final Philosophy

Artifact dispatch protocols are NOT:
generation order rules.

They ARE:

runtime cognition infrastructure orchestration laws
for AI-native presentation systems.
