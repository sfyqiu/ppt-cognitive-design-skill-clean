# Refinement Protocol

## Purpose

This document defines the mandatory runtime refinement execution behavior for the AI-native presentation cognition system.

The objective is NOT:
visual editing.

The objective IS:
precision cognition alignment.

Traditional systems refine through:

```text
feedback
↓
regeneration
```

This runtime refines through:

```text
feedback
↓
cognition diagnosis
↓
localized refinement
↓
continuity preservation
```

Refinement must preserve:

persistent collaboration continuity.

---

# Core Refinement Philosophy

Refinement is NOT:

design modification.

Refinement IS:

communication cognition correction.

The runtime MUST preserve:

- workflow identity
- presentation identity
- audience alignment
- semantic continuity
- renderer continuity
- refinement history

Refinement must remain:

localized and explainable.

---

# Global Refinement Flow

```text
User Feedback
↓
Feedback Interpretation
↓
Cognition Failure Detection
↓
Diagnostics
↓
Local Scope Detection
↓
Renderer Coordination
↓
Refinement Patch
↓
Evaluation
↓
Cognition Diff
↓
Refinement Trace
↓
Persistent Continuation
```

Refinement becomes:

persistent cognition evolution.

---

# Protocol 1 — Feedback Interpretation

## Requirement

The runtime MUST interpret:

feedback cognitively.

---

## Mandatory Behavior

Example:

```text
too messy
```

↓

```yaml
interpreted_problem:
  hierarchy_conflict
  density_overload
```

The runtime MUST NOT:

interpret feedback literally only.

---

# Protocol 2 — Diagnostics Before Refinement

## Requirement

The runtime MUST diagnose:

before modifying.

---

## Mandatory Detection

```yaml
detect:
  - hierarchy_failure
  - density_failure
  - mechanism_confusion
  - audience_mismatch
```

Diagnostics regulate:

refinement behavior.

---

# Protocol 3 — Local Scope Detection

## Requirement

The runtime MUST localize:

refinement boundaries.

---

## Mandatory Behavior

```yaml
affected_regions:
  - annotation_layer

preserve_regions:
  - narrative_structure
```

The runtime MUST NOT:

default to global redesign.

---

# Protocol 4 — Renderer Coordination

## Requirement

The runtime MUST coordinate:

renderers selectively.

---

## Mandatory Behavior

```yaml
activate:
  - hierarchy_renderer
  - adaptive_layout_engine
```

The runtime MUST preserve:

renderer continuity.

---

# Protocol 5 — Refinement Patch Generation

## Requirement

The runtime MUST generate:

refinement_patch.

---

## Mandatory Patch Structure

```yaml
refine:
  - spacing
  - hierarchy
  - mechanism_visibility

preserve:
  - semantic_behavior
  - workflow_identity
```

Patches preserve:

precision refinement behavior.

---

# Protocol 6 — Cognition Diff Generation

## Requirement

The runtime MUST generate:

cognition_diff.

---

## Mandatory Purpose

The cognition diff MUST explain:

```yaml
explain:
  - what_changed
  - why_changed
  - communication_improvement
```

Diffs preserve:

refinement explainability.

---

# Protocol 7 — Refinement Trace Preservation

## Requirement

The runtime MUST update:

refinement_trace continuously.

---

## Mandatory Purpose

The refinement trace preserves:

```yaml
preserve:
  - refinement_history
  - diagnostics_history
  - workflow_continuity
```

Refinement becomes:

persistent cognition evolution.

---

# Protocol 8 — Persistent Collaboration Preservation

## Requirement

The runtime MUST preserve:

persistent collaboration continuity.

---

## Mandatory State

```yaml
persistent_state:

  workflow_identity:
  presentation_identity:
  refinement_history:
```

The runtime MUST behave as:

a persistent collaborator.

---

# Refinement Rules

The runtime MUST:

- diagnose before refining
- preserve workflow continuity
- preserve presentation identity
- localize modifications
- preserve semantic meaning
- preserve audience alignment
- maintain explainability

The runtime MUST NOT:

- redesign unnecessarily
- reset cognition state
- fragment workflows
- introduce semantic drift

---

# Anti-AI-Slop Refinement Protection

The runtime MUST prevent:

```yaml
prevent:
  - redesign_loops
  - decorative_refinement
  - infographic_escalation
  - random_visual_changes
  - stateless_revision_behavior
```

Refinement should feel:

editorial,
intentional,
and cognition-aware.

---

# Refinement Validation

```yaml
validate:
  - diagnostics_completed
  - refinement_localized
  - workflow_continuity_preserved
  - semantic_consistency_preserved
  - audience_alignment_preserved
  - explainability_preserved
```

If NO:

refinement behavior is invalid.

---

# System Identity

This refinement protocol is NOT:

editing guidance.

It IS:

persistent cognition refinement infrastructure.

---

# Final Philosophy

Refinement protocols are NOT:
revision instructions.

They ARE:

precision cognition correction laws
for AI-native presentation systems.
