# Skill Runtime Protocol

## Purpose

This document defines the mandatory execution protocol used when an agent enters the AI-native presentation cognition skill runtime.

The objective is NOT:
prompt execution.

The objective IS:
controlled cognition runtime orchestration.

The skill runtime protocol ensures:

every agent invoking the skill
follows cognition runtime laws.

---

# Core Skill Runtime Philosophy

A skill is NOT:

a reusable prompt.

A skill IS:

a cognition operating environment.

Agents entering the skill MUST:

- initialize cognition state
- dispatch workflows
- establish audience reasoning
- coordinate renderers
- preserve persistent collaboration

The skill runtime MUST behave as:

a cognition runtime system.

---

# Global Skill Runtime Flow

Every skill execution MUST follow:

```text
Skill Entry
↓
Intent Interpretation
↓
Workflow Dispatch
↓
Audience Reasoning
↓
Cognition Planning
↓
Artifact Generation
↓
Renderer Coordination
↓
Rendering Execution
↓
Diagnostics
↓
Refinement
↓
Persistent Collaboration
```

The runtime MUST preserve:

communication cognition continuity.

---

# Protocol 1 — Runtime Initialization

## Requirement

The skill MUST initialize:

persistent cognition state.

---

## Mandatory Initialization

```yaml
initialize:
  - workflow_identity
  - audience_model
  - communication_goal
  - presentation_identity
  - semantic_behavior
```

Generation MUST NOT begin before:

runtime initialization completes.

---

# Protocol 2 — Workflow Dispatch

## Requirement

The skill MUST establish:

one dominant workflow.

---

## Mandatory Behavior

```yaml
workflow_dispatch:

  primary_workflow:

  optional_secondary_workflow:
```

The runtime MUST NOT:

```yaml
allow:
  equal_weight_workflow_blending
```

Workflow stability is:

mandatory.

---

# Protocol 3 — Audience Reasoning

## Requirement

The skill MUST establish:

audience cognition models.

---

## Mandatory Behavior

```yaml
audience_reasoning:

  audience:
  cognition_density:
  accessibility_strategy:
```

Audience reasoning regulates:

all downstream rendering behavior.

---

# Protocol 4 — Cognition Planning

## Requirement

The skill MUST generate:

cognition planning structures.

---

## Mandatory Artifact

```yaml
generate:
  cognition_plan
```

The cognition plan MUST define:

```yaml
define:
  - communication_goal
  - hierarchy_strategy
  - narrative_strategy
  - audience_alignment
```

Planning precedes:

rendering.

---

# Protocol 5 — Artifact Generation

## Requirement

The skill MUST generate:

runtime cognition artifacts.

---

## Mandatory Artifacts

```yaml
generate:
  - slide_blueprint
  - rendering_json
  - adaptive_layout_spec
```

Artifacts become:

runtime cognition infrastructure.

---

# Protocol 6 — Renderer Coordination

## Requirement

The skill MUST coordinate:

renderers through workflow cognition.

---

## Mandatory Behavior

```yaml
renderer_coordination:

  activate:
    - hierarchy_renderer
    - semantic_renderer
    - adaptive_layout_engine
```

Renderers MUST remain:

subordinate to cognition reasoning.

---

# Protocol 7 — Diagnostics Before Refinement

## Requirement

The skill MUST diagnose:

before refining.

---

## Mandatory Diagnostics

```yaml
detect:
  - hierarchy_failure
  - density_failure
  - mechanism_confusion
  - audience_mismatch
```

The runtime MUST NOT:

```yaml
default_to:
  regeneration
```

Diagnostics precede:

refinement.

---

# Protocol 8 — Localized Refinement

## Requirement

The skill MUST prioritize:

localized cognition refinement.

---

## Mandatory Behavior

```yaml
refine:
  local_components

preserve:
  - workflow_identity
  - presentation_identity
  - semantic_behavior
```

The runtime MUST NOT:

```yaml
allow:
  uncontrolled_redesign
```

Refinement must remain:

precision-oriented.

---

# Protocol 9 — Persistent Collaboration

## Requirement

The skill MUST preserve:

persistent cognition continuity.

---

## Mandatory Memory State

```yaml
persistent_state:

  workflow_identity:
  audience_model:
  semantic_behavior:
  refinement_history:
```

The runtime MUST behave as:

a persistent cognition collaborator.

---

# Protocol 10 — Anti-AI-Slop Runtime Protection

## Requirement

The skill MUST actively prevent:

AI-generated cognition degradation.

---

## Mandatory Prevention

```yaml
prevent:
  - startup_pitch_aesthetics
  - infographic_fragmentation
  - template_behavior
  - decorative_overdesign
  - workflow_fragmentation
  - stateless_generation
```

The skill should feel:

editorial,
intentional,
and cognition-aware.

---

# Runtime Validation

Before runtime continuation:

```yaml
validate:
  - workflow_stability
  - audience_alignment
  - hierarchy_clarity
  - semantic_consistency
  - renderer_coordination
  - refinement_precision
  - persistent_collaboration
```

If NO:

runtime behavior is invalid.

---

# System Identity

This skill runtime protocol is NOT:

prompt orchestration.

It IS:

cognition runtime governance infrastructure.

---

# Final Philosophy

Skill runtime protocols are NOT:
execution instructions.

They ARE:

persistent cognition orchestration laws
for AI-native presentation systems.
