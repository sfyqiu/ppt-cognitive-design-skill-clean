```markdown
# Adaptive Layout Engine System

## Purpose

This document defines the adaptive layout engine used by the AI-native presentation cognition system.

The objective is NOT:
fixed template placement.

The objective IS:
cognition-aware layout adaptation.

Traditional layout systems operate as:

```text
template
↓
content insertion
↓
static rendering
```

This system operates as:

```text
communication cognition
↓
hierarchy reasoning
↓
audience adaptation
↓
dynamic layout orchestration
↓
rendering execution
```

The layout engine must adapt layouts based on:

- communication goals
- hierarchy behavior
- audience cognition
- mechanism complexity
- narrative pacing
- density requirements

Layouts should emerge from:

communication intent,
NOT templates.

---

# Core Layout Philosophy

A layout is NOT:

visual arrangement.

A layout IS:

cognitive navigation architecture.

The layout engine should determine:

- where attention stabilizes
- how reading progresses
- where whitespace reduces overload
- how mechanisms become readable
- how interpretation remains smooth

Layouts should feel:

intentional,
editorial,
and cognitively stable.

---

# Global Adaptive Layout Flow

The adaptive layout engine executes:

```text
Cognition Schema
↓
Hierarchy Interpretation
↓
Audience Modeling
↓
Density Analysis
↓
Mechanism Complexity Detection
↓
Narrative Rhythm Planning
↓
Adaptive Layout Generation
↓
Rendering Execution
↓
Layout Validation
```

Layouts should preserve:

communication clarity.

---

# Core Adaptive Layout Schema

Every adaptive layout engine should regulate:

```yaml
layout_identity:
hierarchy_adaptation:
audience_adaptation:
mechanism_adaptation:
density_adaptation:
narrative_adaptation:
spacing_adaptation:
visual_balance_behavior:
stability_behavior:
evaluation_behavior:
```

The layout engine should preserve:

cognition-first communication.

---

# Stage 1 — Layout Identity

## layout_identity

Purpose:

define adaptive layout philosophy.

Example:

```yaml
layout_identity:
  cognition_driven_layout_engine
```

The layout engine should behave as:

a communication orchestration system.

---

# Stage 2 — Hierarchy Adaptation

## hierarchy_adaptation

Purpose:

adapt layouts according to hierarchy cognition.

Example:

```yaml
hierarchy_adaptation:

  dominant_focus:
    centralized

  secondary_information:
    restrained

  focal_competition:
    minimized
```

Layouts should enforce:

clear attention guidance.

---

# Stage 3 — Audience Adaptation

## audience_adaptation

Purpose:

adapt layouts according to audience cognition.

Example:

```yaml
audience_adaptation:

  undergraduate_students:

    enlarge:
      diagrams
      spacing

    simplify:
      visual_density

  researchers:

    preserve:
      scientific_hierarchy
      evidence_visibility
```

Audience cognition regulates:

layout complexity.

---

# Stage 4 — Mechanism Adaptation

## mechanism_adaptation

Purpose:

adapt layouts for mechanism readability.

Example:

```yaml
mechanism_adaptation:

  mechanism_flow:
    directional

  pathway_overlap:
    minimized

  interaction_visibility:
    enhanced
```

Mechanism layouts should prioritize:

causal interpretability.

---

# Stage 5 — Density Adaptation

## density_adaptation

Purpose:

regulate cognitive load.

Example:

```yaml
density_adaptation:

  whitespace_priority:
    high

  simultaneous_focus:
    minimized

  annotation_density:
    controlled
```

Density adaptation improves:

interpretation comfort.

---

# Stage 6 — Narrative Adaptation

## narrative_adaptation

Purpose:

adapt layouts to communication pacing.

Example:

```yaml
narrative_adaptation:

  transitions:
    progressive

  pacing:
    stabilized

  narrative_flow:
    preserved
```

Narrative layouts should feel:

smooth and intentional.

---

# Stage 7 — Spacing Adaptation

## spacing_adaptation

Purpose:

regulate breathing rhythm.

Example:

```yaml
spacing_adaptation:

  spacing_style:
    relaxed_editorial

  compression:
    prevented

  visual_calmness:
    prioritized
```

Whitespace supports:

cognitive stability.

---

# Stage 8 — Visual Balance Behavior

## visual_balance_behavior

Purpose:

regulate layout equilibrium.

Example:

```yaml
visual_balance_behavior:

  visual_weight:
    hierarchy_guided

  symmetry:
    restrained

  decorative_balance:
    minimized
```

Balance should support:

communication clarity,
NOT aesthetic perfection.

---

# Stage 9 — Stability Behavior

## stability_behavior

Purpose:

preserve layout continuity.

Example:

```yaml
stability_behavior:

  preserve:
    - spacing_rhythm
    - editorial_tone
    - hierarchy_consistency

  prevent:
    - random_layout_switching
    - visual_instability
```

Stable layouts improve:

presentation continuity.

---

# Stage 10 — Evaluation Behavior

## evaluation_behavior

Purpose:

validate adaptive layout quality.

Example:

```yaml
evaluation_behavior:

  validate:
    - hierarchy_visibility
    - audience_readability
    - mechanism_clarity
    - density_control
    - narrative_smoothness
    - whitespace_balance
```

Layout quality should be evaluated through:

communication effectiveness.

---

# Full Adaptive Layout Example

```yaml
layout_identity:
  cognition_driven_layout_engine

hierarchy_adaptation:

  dominant_focus:
    centralized

  secondary_information:
    restrained

  focal_competition:
    minimized

audience_adaptation:

  undergraduate_students:

    enlarge:
      diagrams
      spacing

    simplify:
      visual_density

  researchers:

    preserve:
      scientific_hierarchy

mechanism_adaptation:

  mechanism_flow:
    directional

  interaction_visibility:
    enhanced

density_adaptation:

  whitespace_priority:
    high

  annotation_density:
    controlled

narrative_adaptation:

  transitions:
    progressive

  pacing:
    stabilized

spacing_adaptation:

  spacing_style:
    relaxed_editorial

  compression:
    prevented

visual_balance_behavior:

  visual_weight:
    hierarchy_guided

  symmetry:
    restrained

stability_behavior:

  preserve:
    - spacing_rhythm
    - hierarchy_consistency

  prevent:
    - layout_instability

evaluation_behavior:

  validate:
    - hierarchy_visibility
    - mechanism_clarity
    - density_control
    - whitespace_balance
```

---

# Adaptive Layout Rules

The adaptive layout engine must:

- adapt layouts from cognition
- preserve hierarchy visibility
- regulate density
- stabilize narrative pacing
- enhance mechanism readability
- adapt to audience cognition
- maintain editorial calmness

Layouts should NEVER:

behave as static templates.

---

# Anti-AI-Slop Layout Rules

The layout engine must actively prevent:

- template repetition
- infographic fragmentation
- startup-style compositions
- dashboard layouts
- decorative visual balancing
- excessive visual symmetry
- crowded slide structures

Layouts should feel:

editorially grounded,
communication-oriented,
and human-designed.

---

# Final Validation

Before rendering output:

Check:

- Does the layout support communication goals?
- Is hierarchy obvious?
- Is whitespace sufficient?
- Is audience readability preserved?
- Is mechanism flow readable?
- Is density controlled?
- Does the layout feel adaptive rather than templated?

If NO:

refine adaptive layout behavior.

---

# System Identity

This adaptive layout engine is NOT:

a template system.

It IS:

a cognition-aware layout orchestration engine.

---

# Final Philosophy

Adaptive layout is NOT:
visual arrangement automation.

It IS:

communication cognition adaptation
for audience understanding systems.
```
