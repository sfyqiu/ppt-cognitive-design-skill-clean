```markdown
# Hierarchy Renderer System

## Purpose

This document defines the hierarchy-aware rendering engine used by the AI-native presentation cognition system.

The objective is NOT:
visual balancing.

The objective IS:
audience attention orchestration.

Traditional rendering systems attempt to:

- distribute objects evenly
- maximize visual fullness
- optimize aesthetic symmetry

This system prioritizes:

- dominant focus clarity
- reading guidance
- cognitive prioritization
- communication sequencing
- attention stabilization

The hierarchy renderer exists to ensure:

audiences immediately understand
where attention should go first.

---

# Core Hierarchy Philosophy

Hierarchy is NOT:

visual decoration.

Hierarchy IS:

controlled cognitive emphasis.

Every slide should answer:

```text
What should the audience notice first?
```

The renderer must guide:

```text
Attention
↓
Interpretation
↓
Understanding
↓
Memory
```

The renderer should create:

visual inevitability.

---

# Global Hierarchy Rendering Flow

The hierarchy renderer executes:

```text
Hierarchy Schema
↓
Dominant Focus Detection
↓
Reading Order Planning
↓
Visual Weight Allocation
↓
Whitespace Stabilization
↓
Attention Regulation
↓
Rendering Execution
↓
Hierarchy Validation
```

Hierarchy rendering should prioritize:

communication clarity.

---

# Core Hierarchy Renderer Schema

Every hierarchy renderer should regulate:

```yaml
hierarchy_identity:
focus_behavior:
reading_behavior:
visual_weight_behavior:
spacing_behavior:
annotation_behavior:
evidence_behavior:
takeaway_behavior:
stability_behavior:
evaluation_behavior:
```

The renderer should preserve:

attention hierarchy.

---

# Stage 1 — Hierarchy Identity

## hierarchy_identity

Purpose:

define hierarchy rendering philosophy.

Example:

```yaml
hierarchy_identity:
  cognition_priority_hierarchy
```

The hierarchy renderer should behave as:

an attention orchestration system.

---

# Stage 2 — Focus Behavior

## focus_behavior

Purpose:

regulate dominant visual focus.

Example:

```yaml
focus_behavior:

  dominant_focus:
    enforced

  competing_focus:
    minimized

  focal_visibility:
    maximized
```

The renderer must ensure:

one dominant anchor.

The audience should identify it within:

3 seconds.

---

# Stage 3 — Reading Behavior

## reading_behavior

Purpose:

regulate audience eye movement.

Example:

```yaml
reading_behavior:

  reading_order:
    top_to_bottom

  transitions:
    smooth

  interpretation_guidance:
    preserved
```

Reading flow should feel:

natural and intentional.

---

# Stage 4 — Visual Weight Behavior

## visual_weight_behavior

Purpose:

regulate hierarchy emphasis.

Example:

```yaml
visual_weight_behavior:

  primary:
    dominant

  secondary:
    restrained

  tertiary:
    subtle
```

Visual weight should regulate:

- scale
- contrast
- spacing
- emphasis

The system must prevent:

equal-weight competition.

---

# Stage 5 — Spacing Behavior

## spacing_behavior

Purpose:

regulate whitespace hierarchy.

Example:

```yaml
spacing_behavior:

  whitespace_priority:
    high

  breathing_rhythm:
    relaxed_editorial

  compression:
    prevented
```

Whitespace improves:

cognitive calmness.

---

# Stage 6 — Annotation Behavior

## annotation_behavior

Purpose:

regulate annotation hierarchy.

Example:

```yaml
annotation_behavior:

  annotation_density:
    restrained

  explanatory_support:
    preserved

  annotation_competition:
    minimized
```

Annotations should support:

interpretation,
NOT dominate slides.

---

# Stage 7 — Evidence Behavior

## evidence_behavior

Purpose:

regulate evidence hierarchy.

Example:

```yaml
evidence_behavior:

  evidence_priority:
    secondary

  mechanism_support:
    emphasized

  evidence_overload:
    prevented
```

Evidence should reinforce:

communication goals.

---

# Stage 8 — Takeaway Behavior

## takeaway_behavior

Purpose:

regulate memory reinforcement.

Example:

```yaml
takeaway_behavior:

  dominant_takeaway:
    reinforced

  takeaway_visibility:
    stable

  memory_anchor:
    preserved
```

Every slide should preserve:

one memorable takeaway.

---

# Stage 9 — Stability Behavior

## stability_behavior

Purpose:

preserve hierarchy continuity.

Example:

```yaml
stability_behavior:

  preserve:
    - typography_system
    - spacing_rhythm
    - hierarchy_consistency

  prevent:
    - visual_instability
    - hierarchy_drift
```

Stable hierarchy improves:

presentation continuity.

---

# Stage 10 — Evaluation Behavior

## evaluation_behavior

Purpose:

validate hierarchy rendering quality.

Example:

```yaml
evaluation_behavior:

  validate:
    - dominant_focus_visibility
    - reading_order_clarity
    - whitespace_balance
    - hierarchy_stability
    - annotation_control
```

Hierarchy quality should be evaluated through:

attention guidance effectiveness.

---

# Full Hierarchy Renderer Example

```yaml
hierarchy_identity:
  cognition_priority_hierarchy

focus_behavior:

  dominant_focus:
    enforced

  competing_focus:
    minimized

  focal_visibility:
    maximized

reading_behavior:

  reading_order:
    top_to_bottom

  transitions:
    smooth

visual_weight_behavior:

  primary:
    dominant

  secondary:
    restrained

  tertiary:
    subtle

spacing_behavior:

  whitespace_priority:
    high

  breathing_rhythm:
    relaxed_editorial

annotation_behavior:

  annotation_density:
    restrained

  explanatory_support:
    preserved

evidence_behavior:

  evidence_priority:
    secondary

  mechanism_support:
    emphasized

takeaway_behavior:

  dominant_takeaway:
    reinforced

  memory_anchor:
    preserved

stability_behavior:

  preserve:
    - typography_system
    - hierarchy_consistency

  prevent:
    - visual_drift

evaluation_behavior:

  validate:
    - dominant_focus_visibility
    - hierarchy_stability
    - reading_order_clarity
    - whitespace_balance
```

---

# Hierarchy Rendering Rules

The hierarchy renderer must:

- enforce one dominant focus
- preserve reading order
- regulate visual weight
- stabilize spacing rhythm
- subordinate evidence
- minimize annotation competition
- reinforce communication takeaways

Hierarchy rendering should prioritize:

attention guidance.

---

# Anti-AI-Slop Hierarchy Rules

The renderer must actively prevent:

- equal-weight layouts
- infographic chaos
- dashboard fragmentation
- excessive emphasis competition
- decorative visual overload
- random focal placement

Slides should feel:

editorially restrained,
human-guided,
and cognitively stable.

---

# Final Validation

Before rendering output:

Check:

- Is there one dominant focus?
- Is reading order obvious?
- Is hierarchy stable?
- Is whitespace sufficient?
- Are annotations restrained?
- Does evidence support rather than compete?
- Does the slide guide attention naturally?

If NO:

refine hierarchy rendering behavior.

---

# System Identity

This hierarchy renderer is NOT:

visual balancing logic.

It IS:

audience attention orchestration rendering.

---

# Final Philosophy

Hierarchy rendering is NOT:
object arrangement.

It IS:

controlled cognitive prioritization
for audience understanding systems.
```
