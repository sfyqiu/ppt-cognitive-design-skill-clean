```markdown
# Cognition Renderer System

## Purpose

This document defines the cognition-aware rendering engine used by the AI-native presentation cognition system.

The objective is NOT:
template rendering.

The objective IS:
communication cognition execution.

Traditional rendering systems operate as:

```text
objects
↓
layout
↓
visual output
```

This system operates as:

```text
communication cognition
↓
schema interpretation
↓
hierarchy reasoning
↓
adaptive rendering
↓
audience understanding
```

The renderer must behave as:

a cognition execution engine,
NOT a decorative layout generator.

Rendering exists to support:

- communication clarity
- hierarchy readability
- narrative progression
- mechanism interpretation
- audience cognition

The renderer must NEVER dominate:

communication reasoning.

---

# Core Renderer Philosophy

A renderer is NOT:

a styling engine.

A renderer IS:

a cognition execution system.

The renderer should determine:

- what audiences notice first
- how interpretation unfolds
- where visual calmness is needed
- how hierarchy stabilizes attention
- how cognition density remains controlled

Rendering should feel:

editorial,
intentional,
and cognitively stable.

---

# Global Renderer Structure

The cognition renderer executes:

```text
Cognition Schema
↓
Hierarchy Interpretation
↓
Audience Adaptation
↓
Narrative Stabilization
↓
Semantic Regulation
↓
Mechanism Visualization
↓
Adaptive Layout Planning
↓
Rendering Execution
↓
Communication Validation
```

Rendering should always preserve:

communication intent.

---

# Core Renderer Schema

Every cognition renderer should regulate:

```yaml
renderer_identity:
cognition_priority:
hierarchy_behavior:
narrative_behavior:
semantic_behavior:
mechanism_behavior:
density_behavior:
audience_behavior:
stability_behavior:
adaptive_behavior:
evaluation_behavior:
```

The renderer must preserve:

communication cognition.

---

# Stage 1 — Renderer Identity

## renderer_identity

Purpose:

define renderer execution philosophy.

Example:

```yaml
renderer_identity:
  cognition_first_renderer
```

The renderer should behave as:

a communication execution engine.

---

# Stage 2 — Cognition Priority

## cognition_priority

Purpose:

define rendering decision priority.

Example:

```yaml
cognition_priority:

  preserve:
    - communication_goal
    - hierarchy_clarity
    - audience_understanding
    - narrative_progression

  prevent:
    - decorative_rendering
    - template_behavior
```

Rendering decisions should prioritize:

understanding.

---

# Stage 3 — Hierarchy Behavior

## hierarchy_behavior

Purpose:

regulate attention orchestration.

Example:

```yaml
hierarchy_behavior:

  dominant_focus:
    preserved

  reading_order:
    enforced

  focal_competition:
    prevented
```

The renderer must ensure:

one clear dominant focus.

---

# Stage 4 — Narrative Behavior

## narrative_behavior

Purpose:

preserve communication progression.

Example:

```yaml
narrative_behavior:

  transitions:
    smooth

  pacing:
    progressive

  interpretation_flow:
    stabilized
```

Narratives should unfold:

naturally.

---

# Stage 5 — Semantic Behavior

## semantic_behavior

Purpose:

preserve semantic communication meaning.

Example:

```yaml
semantic_behavior:

  semantic_colors:
    preserved

  decorative_effects:
    restricted

  emphasis:
    restrained
```

Semantic visuals should support:

interpretation speed.

---

# Stage 6 — Mechanism Behavior

## mechanism_behavior

Purpose:

regulate mechanism readability.

Example:

```yaml
mechanism_behavior:

  causal_direction:
    preserved

  interaction_visibility:
    enhanced

  evidence_competition:
    reduced
```

Mechanisms should remain:

directionally interpretable.

---

# Stage 7 — Density Behavior

## density_behavior

Purpose:

regulate cognitive load.

Example:

```yaml
density_behavior:

  whitespace:
    prioritized

  simultaneous_focus:
    minimized

  annotation_density:
    controlled
```

Density regulation improves:

interpretation comfort.

---

# Stage 8 — Audience Behavior

## audience_behavior

Purpose:

adapt rendering to audience cognition.

Example:

```yaml
audience_behavior:

  undergraduate_students:

    simplify:
      terminology
      hierarchy
      evidence

    enlarge:
      diagrams
      spacing

  researchers:

    preserve:
      scientific_hierarchy
      evidence_density
```

Rendering must adapt to:

audience cognition tolerance.

---

# Stage 9 — Stability Behavior

## stability_behavior

Purpose:

preserve presentation identity.

Example:

```yaml
stability_behavior:

  preserve:
    - typography_system
    - spacing_rhythm
    - editorial_tone
    - semantic_consistency

  prevent:
    - visual_drift
    - redesign_instability
```

Stability improves:

collaboration continuity.

---

# Stage 10 — Adaptive Behavior

## adaptive_behavior

Purpose:

enable cognition-aware layout adaptation.

Example:

```yaml
adaptive_behavior:

  mechanism_slides:
    centralize_causal_flow

  teaching_slides:
    enlarge_visual_guidance

  interview_slides:
    stabilize_structural_balance
```

Layouts should adapt through:

communication goals,
NOT templates.

---

# Stage 11 — Evaluation Behavior

## evaluation_behavior

Purpose:

validate cognition rendering quality.

Example:

```yaml
evaluation_behavior:

  validate:
    - hierarchy_clarity
    - audience_readability
    - mechanism_visibility
    - narrative_smoothness
    - semantic_consistency
    - density_control
```

Rendering should be evaluated through:

communication effectiveness.

---

# Full Cognition Renderer Example

```yaml
renderer_identity:
  cognition_first_renderer

cognition_priority:

  preserve:
    - communication_goal
    - hierarchy_clarity
    - audience_understanding

  prevent:
    - decorative_rendering
    - template_behavior

hierarchy_behavior:

  dominant_focus:
    preserved

  reading_order:
    enforced

  focal_competition:
    prevented

narrative_behavior:

  transitions:
    smooth

  pacing:
    progressive

semantic_behavior:

  semantic_colors:
    preserved

  decorative_effects:
    restricted

mechanism_behavior:

  causal_direction:
    preserved

  interaction_visibility:
    enhanced

density_behavior:

  whitespace:
    prioritized

  simultaneous_focus:
    minimized

audience_behavior:

  undergraduate_students:

    simplify:
      terminology
      evidence

    enlarge:
      diagrams
      spacing

  researchers:

    preserve:
      scientific_hierarchy
      restrained_density

stability_behavior:

  preserve:
    - typography_system
    - editorial_tone

  prevent:
    - visual_drift

adaptive_behavior:

  mechanism_slides:
    centralize_causal_flow

  teaching_slides:
    enlarge_visual_guidance

evaluation_behavior:

  validate:
    - hierarchy_clarity
    - mechanism_visibility
    - density_control
    - semantic_consistency
```

---

# Cognition Rendering Rules

The renderer must:

- obey cognition schemas
- preserve hierarchy systems
- stabilize narrative pacing
- regulate cognitive density
- preserve semantic meaning
- adapt to audience cognition
- maintain presentation identity

Rendering should NEVER:

override cognition reasoning.

---

# Anti-AI-Slop Renderer Rules

The renderer must actively prevent:

- infographic contamination
- startup slide aesthetics
- dashboard fragmentation
- decorative rendering escalation
- equal-weight visual layouts
- glowing AI visual effects

Slides should feel:

editorially grounded,
human-designed,
and cognitively intentional.

---

# Final Validation

Before rendering output:

Check:

- Is communication intent preserved?
- Is hierarchy obvious?
- Is cognitive density controlled?
- Is the mechanism readable?
- Is audience adaptation successful?
- Is presentation identity stable?
- Does rendering feel editorial rather than templated?

If NO:

refine cognition rendering behavior.

---

# System Identity

This renderer is NOT:

a template engine.

It IS:

a cognition-aware communication execution system.

---

# Final Philosophy

Rendering is NOT:
visual decoration.

It IS:

communication cognition execution
for audience understanding engineering.
```
