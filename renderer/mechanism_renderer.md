```markdown
# Mechanism Renderer System

## Purpose

This document defines the mechanism-aware rendering engine used by the AI-native presentation cognition system.

The objective is NOT:
scientific figure decoration.

The objective IS:
causal scientific interpretability.

Traditional scientific rendering systems often produce:

- disconnected mechanisms
- crowded pathways
- annotation overload
- evidence competition
- visually chaotic diagrams

This system prioritizes:

- causal readability
- directional understanding
- interaction clarity
- mechanism hierarchy
- interpretation stability

The mechanism renderer exists to ensure:

audiences understand:

```text
why the process happens
```

NOT only:

```text
what the process contains
```

---

# Core Mechanism Philosophy

Scientific mechanism rendering is NOT:

visual complexity.

It IS:

causal communication engineering.

Mechanism rendering should guide:

```text
Trigger
↓
Environmental Response
↓
Interaction
↓
Functional Change
↓
Outcome
↓
Interpretation
```

The renderer must prioritize:

understanding pathways.

---

# Global Mechanism Rendering Flow

The mechanism renderer executes:

```text
Mechanism Schema
↓
Causal Structure Detection
↓
Directional Flow Planning
↓
Interaction Isolation
↓
Evidence Subordination
↓
Mechanism Visualization
↓
Rendering Execution
↓
Scientific Interpretability Validation
```

Mechanism rendering should prioritize:

causal cognition.

---

# Core Mechanism Renderer Schema

Every mechanism renderer should regulate:

```yaml
mechanism_identity:
causal_behavior:
direction_behavior:
interaction_behavior:
evidence_behavior:
annotation_behavior:
spacing_behavior:
hierarchy_behavior:
scientific_behavior:
stability_behavior:
evaluation_behavior:
```

The renderer should preserve:

mechanism readability.

---

# Stage 1 — Mechanism Identity

## mechanism_identity

Purpose:

define mechanism rendering philosophy.

Example:

```yaml
mechanism_identity:
  causal_interpretation_renderer
```

The mechanism renderer should behave as:

a scientific interpretation system.

---

# Stage 2 — Causal Behavior

## causal_behavior

Purpose:

regulate causal communication clarity.

Example:

```yaml
causal_behavior:

  causal_hierarchy:
    preserved

  trigger_visibility:
    enhanced

  outcome_traceability:
    maintained
```

The renderer must ensure:

audiences can trace:

cause → outcome relationships.

---

# Stage 3 — Direction Behavior

## direction_behavior

Purpose:

regulate mechanism reading direction.

Example:

```yaml
direction_behavior:

  directional_flow:
    stabilized

  reading_path:
    obvious

  flow_fragmentation:
    prevented
```

Directional flow should feel:

inevitable.

---

# Stage 4 — Interaction Behavior

## interaction_behavior

Purpose:

regulate interaction readability.

Example:

```yaml
interaction_behavior:

  interaction_visibility:
    enhanced

  pathway_overlap:
    minimized

  process_isolation:
    preserved
```

Interactions should remain:

visually interpretable.

---

# Stage 5 — Evidence Behavior

## evidence_behavior

Purpose:

regulate scientific evidence hierarchy.

Example:

```yaml
evidence_behavior:

  evidence_priority:
    secondary

  mechanism_support:
    emphasized

  evidence_competition:
    prevented
```

Evidence should support:

causal understanding.

---

# Stage 6 — Annotation Behavior

## annotation_behavior

Purpose:

regulate mechanism explanation density.

Example:

```yaml
annotation_behavior:

  annotation_style:
    restrained

  explanatory_guidance:
    preserved

  annotation_noise:
    minimized
```

Annotations should clarify:

mechanisms,
NOT clutter diagrams.

---

# Stage 7 — Spacing Behavior

## spacing_behavior

Purpose:

regulate mechanism breathing space.

Example:

```yaml
spacing_behavior:

  whitespace_priority:
    high

  pathway_separation:
    preserved

  visual_compression:
    prevented
```

Spacing improves:

mechanism readability.

---

# Stage 8 — Hierarchy Behavior

## hierarchy_behavior

Purpose:

regulate mechanism hierarchy visibility.

Example:

```yaml
hierarchy_behavior:

  dominant_mechanism:
    enforced

  secondary_information:
    restrained

  focal_competition:
    minimized
```

Mechanism hierarchy should guide:

scientific interpretation.

---

# Stage 9 — Scientific Behavior

## scientific_behavior

Purpose:

preserve editorial scientific communication.

Example:

```yaml
scientific_behavior:

  editorial_tone:
    preserved

  decorative_effects:
    restricted

  scientific_restraint:
    maintained
```

Scientific visuals should feel:

publication-oriented.

---

# Stage 10 — Stability Behavior

## stability_behavior

Purpose:

preserve mechanism rendering continuity.

Example:

```yaml
stability_behavior:

  preserve:
    - directional_consistency
    - spacing_rhythm
    - editorial_scientific_tone

  prevent:
    - infographic_drift
    - visual_instability
```

Mechanism continuity improves:

scientific professionalism.

---

# Stage 11 — Evaluation Behavior

## evaluation_behavior

Purpose:

validate mechanism rendering quality.

Example:

```yaml
evaluation_behavior:

  validate:
    - causal_readability
    - directional_clarity
    - interaction_visibility
    - hierarchy_stability
    - evidence_subordination
    - annotation_control
```

Mechanism quality should be evaluated through:

scientific interpretability.

---

# Full Mechanism Renderer Example

```yaml
mechanism_identity:
  causal_interpretation_renderer

causal_behavior:

  causal_hierarchy:
    preserved

  trigger_visibility:
    enhanced

  outcome_traceability:
    maintained

direction_behavior:

  directional_flow:
    stabilized

  reading_path:
    obvious

interaction_behavior:

  interaction_visibility:
    enhanced

  pathway_overlap:
    minimized

evidence_behavior:

  evidence_priority:
    secondary

  mechanism_support:
    emphasized

annotation_behavior:

  annotation_style:
    restrained

  explanatory_guidance:
    preserved

spacing_behavior:

  whitespace_priority:
    high

  pathway_separation:
    preserved

hierarchy_behavior:

  dominant_mechanism:
    enforced

  secondary_information:
    restrained

scientific_behavior:

  editorial_tone:
    preserved

  scientific_restraint:
    maintained

stability_behavior:

  preserve:
    - directional_consistency
    - editorial_scientific_tone

  prevent:
    - infographic_drift

evaluation_behavior:

  validate:
    - causal_readability
    - interaction_visibility
    - hierarchy_stability
    - annotation_control
```

---

# Mechanism Rendering Rules

The mechanism renderer must:

- preserve causal flow
- enforce directional readability
- isolate interactions clearly
- subordinate evidence
- regulate annotation density
- stabilize mechanism hierarchy
- maintain scientific editorial tone

Mechanism rendering should prioritize:

scientific understanding.

---

# Anti-AI-Slop Mechanism Rules

The renderer must actively prevent:

- infographic-style mechanisms
- decorative arrows
- chaotic interaction layouts
- evidence overload
- annotation flooding
- startup-style scientific visuals
- decorative scientific effects

Mechanism slides should feel:

editorially scientific,
causally interpretable,
and human-designed.

---

# Final Validation

Before rendering output:

Check:

- Is the trigger obvious?
- Is directional flow clear?
- Are interactions readable?
- Is evidence subordinate to mechanisms?
- Is annotation density controlled?
- Does the slide explain why rather than only what?
- Does rendering preserve scientific professionalism?

If NO:

refine mechanism rendering behavior.

---

# System Identity

This mechanism renderer is NOT:

scientific figure styling.

It IS:

causal scientific cognition rendering.

---

# Final Philosophy

Mechanism rendering is NOT:
visual complexity.

It IS:

causal understanding execution
for scientific communication systems.
```
