```markdown
# Workflow Dispatcher System

## Purpose

This document defines the workflow dispatcher used by the AI-native presentation cognition runtime.

The objective is NOT:
keyword-based routing.

The objective IS:
cognition-aware workflow activation.

Traditional systems dispatch workflows through:

- prompt categories
- rigid conditions
- static rules
- template matching

This system dispatches workflows through:

- communication intention
- audience cognition
- presentation state
- refinement context
- cognition failure analysis

The workflow dispatcher exists to determine:

```text
Which cognition workflow should dominate runtime behavior.
```

---

# Core Dispatcher Philosophy

The system should NOT ask:

```text
What template should be used?
```

The system SHOULD ask:

```text
What cognition behavior is required?
```

The dispatcher must coordinate:

```text
User Intention
↓
Communication Goal
↓
Audience Context
↓
Cognition State
↓
Workflow Selection
↓
Runtime Coordination
```

Workflow selection should feel:

intentional,
stable,
and cognition-aware.

---

# Global Dispatch Flow

The workflow dispatcher executes:

```text
User Input
↓
Intent Interpretation
↓
Audience Detection
↓
Presentation State Analysis
↓
Cognition Failure Detection
↓
Workflow Priority Determination
↓
Primary Workflow Selection
↓
Secondary Workflow Support
↓
Runtime Activation
```

The dispatcher should preserve:

workflow stability.

---

# Core Workflow Dispatcher Schema

Every workflow dispatcher should regulate:

```yaml
dispatcher_identity:
intent_detection:
audience_detection:
state_detection:
failure_detection:
workflow_selection:
secondary_support_behavior:
revision_dispatch_behavior:
stability_behavior:
evaluation_behavior:
```

The dispatcher should preserve:

cognition-oriented workflow routing.

---

# Stage 1 — Dispatcher Identity

## dispatcher_identity

Purpose:

define dispatch philosophy.

Example:

```yaml
dispatcher_identity:
  cognition_aware_workflow_dispatcher
```

The dispatcher should behave as:

a cognition routing system.

---

# Stage 2 — Intent Detection

## intent_detection

Purpose:

detect communication objectives.

Example:

```yaml
intent_detection:

  detect:
    - teaching_goal
    - scientific_explanation
    - interview_positioning
    - refinement_request
    - mechanism_clarity_problem

  prevent:
    literal_keyword_routing
```

The dispatcher should interpret:

communication intention.

---

# Stage 3 — Audience Detection

## audience_detection

Purpose:

detect audience cognition requirements.

Example:

```yaml
audience_detection:

  undergraduate_students:
    prioritize:
      teaching_workflow

  researchers:
    prioritize:
      academic_workflow

  faculty_committee:
    prioritize:
      interview_workflow
```

Audience cognition regulates:

workflow dominance.

---

# Stage 4 — State Detection

## state_detection

Purpose:

detect presentation runtime state.

Example:

```yaml
state_detection:

  detect:
    - initial_generation
    - local_revision
    - hierarchy_failure
    - density_overload
    - narrative_instability
```

State detection regulates:

runtime behavior.

---

# Stage 5 — Failure Detection

## failure_detection

Purpose:

detect cognition communication failures.

Example:

```yaml
failure_detection:

  hierarchy_failure:
    activate:
      hierarchy_renderer

  mechanism_confusion:
    activate:
      mechanism_renderer

  audience_mismatch:
    activate:
      teaching_workflow
```

The dispatcher should identify:

root cognition problems.

---

# Stage 6 — Workflow Selection

## workflow_selection

Purpose:

determine dominant workflow behavior.

Example:

```yaml
workflow_selection:

  primary:
    academic_workflow

  secondary:
    teaching_workflow

  preserve:
    dominant_behavior

  prevent:
    equal_weight_blending
```

One workflow must dominate:

runtime cognition behavior.

---

# Stage 7 — Secondary Support Behavior

## secondary_support_behavior

Purpose:

coordinate workflow assistance.

Example:

```yaml
secondary_support_behavior:

  teaching_support:

    improve:
      readability
      diagram_guidance
      cognitive_accessibility

  academic_support:

    improve:
      mechanism_logic
      evidence_hierarchy
```

Secondary workflows should support:

primary communication goals.

---

# Stage 8 — Revision Dispatch Behavior

## revision_dispatch_behavior

Purpose:

coordinate refinement workflow routing.

Example:

```yaml
revision_dispatch_behavior:

  local_revision:
    preserve:
      presentation_identity

    activate:
      refinement_workflow

  global_redesign:
    activate:
      planning_workflow
```

Revisions should remain:

localized whenever possible.

---

# Stage 9 — Stability Behavior

## stability_behavior

Purpose:

preserve dispatch continuity.

Example:

```yaml
stability_behavior:

  preserve:
    - workflow_identity
    - audience_model
    - presentation_tone

  prevent:
    - workflow_instability
    - routing_fragmentation
```

Dispatch stability improves:

persistent collaboration.

---

# Stage 10 — Evaluation Behavior

## evaluation_behavior

Purpose:

validate dispatch effectiveness.

Example:

```yaml
evaluation_behavior:

  validate:
    - workflow_alignment
    - audience_alignment
    - cognition_stability
    - refinement_precision
```

Dispatch quality should be evaluated through:

communication coordination effectiveness.

---

# Full Workflow Dispatcher Example

```yaml
dispatcher_identity:
  cognition_aware_workflow_dispatcher

intent_detection:

  detect:
    - scientific_explanation
    - refinement_request
    - hierarchy_problem

  prevent:
    literal_keyword_routing

audience_detection:

  researchers:
    prioritize:
      academic_workflow

  undergraduate_students:
    prioritize:
      teaching_workflow

state_detection:

  detect:
    - local_revision
    - density_overload
    - mechanism_confusion

failure_detection:

  hierarchy_failure:
    activate:
      hierarchy_renderer

  mechanism_confusion:
    activate:
      mechanism_renderer

workflow_selection:

  primary:
    academic_workflow

  secondary:
    teaching_workflow

  preserve:
    dominant_behavior

secondary_support_behavior:

  teaching_support:

    improve:
      readability
      visual_guidance

revision_dispatch_behavior:

  local_revision:
    preserve:
      presentation_identity

    activate:
      refinement_workflow

stability_behavior:

  preserve:
    - workflow_identity
    - audience_model

  prevent:
    - workflow_fragmentation

evaluation_behavior:

  validate:
    - workflow_alignment
    - audience_alignment
    - cognition_stability
```

---

# Workflow Dispatch Rules

The dispatcher must:

- interpret communication intention
- detect cognition failures
- preserve dominant workflows
- stabilize workflow behavior
- preserve audience adaptation
- localize revisions intelligently
- coordinate renderer activation

Dispatching should prioritize:

cognition alignment.

---

# Anti-AI-Slop Dispatch Rules

The dispatcher must actively prevent:

- keyword-only routing
- random workflow switching
- equal-weight workflow blending
- presentation identity collapse
- unnecessary global regeneration
- workflow fragmentation
- template-driven dispatch logic

The dispatcher should feel:

persistent,
intentional,
and cognition-aware.

---

# Final Validation

Before workflow activation:

Check:

- Is the dominant workflow clear?
- Is audience alignment preserved?
- Is cognition failure correctly identified?
- Is refinement localized?
- Is workflow coordination stable?
- Is presentation identity preserved?
- Does routing feel cognition-aware rather than rule-based?

If NO:

refine workflow dispatch behavior.

---

# System Identity

This workflow dispatcher is NOT:

conditional routing logic.

It IS:

cognition-aware workflow orchestration.

---

# Final Philosophy

Workflow dispatch is NOT:
template routing.

It IS:

communication cognition coordination
for persistent presentation collaboration systems.
```
