```markdown
# Cognition Orchestration Engine System

## Purpose

This document defines the runtime cognition orchestration engine used by the AI-native presentation cognition system.

The objective is NOT:
simple pipeline execution.

The objective IS:
persistent cognition coordination.

Traditional AI systems operate as:

```text
input
↓
generation
↓
output
```

This system operates as:

```text
user intention
↓
cognition orchestration
↓
workflow coordination
↓
schema reasoning
↓
renderer execution
↓
evaluation
↓
iterative refinement
↓
persistent collaboration
```

The orchestration engine exists to coordinate:

- workflows
- skills
- schemas
- renderers
- evaluation systems
- refinement loops
- cognition memory

as one unified cognition runtime.

---

# Core Orchestration Philosophy

The system should NOT behave as:

a generator.

The system SHOULD behave as:

a persistent presentation collaborator.

The orchestration engine must coordinate:

```text
Understanding
↓
Planning
↓
Execution
↓
Evaluation
↓
Refinement
↓
Stabilization
```

The runtime should feel:

continuous,
intentional,
and cognitively stable.

---

# Global Runtime Flow

The orchestration engine executes:

```text
User Intent
↓
Intent Interpretation
↓
Workflow Dispatch
↓
Skill Activation
↓
Cognition State Retrieval
↓
Planning Execution
↓
Schema Construction
↓
Renderer Coordination
↓
Rendering Evaluation
↓
Refinement Decision
↓
Cognition Memory Update
↓
Persistent Collaboration
```

The orchestration engine should preserve:

communication continuity.

---

# Core Orchestration Schema

Every orchestration engine should regulate:

```yaml
orchestration_identity:
intent_behavior:
workflow_behavior:
skill_behavior:
planning_behavior:
schema_behavior:
renderer_behavior:
evaluation_behavior:
refinement_behavior:
memory_behavior:
stability_behavior:
```

The orchestration engine should preserve:

persistent cognition coordination.

---

# Stage 1 — Orchestration Identity

## orchestration_identity

Purpose:

define orchestration philosophy.

Example:

```yaml
orchestration_identity:
  persistent_cognition_runtime
```

The orchestration engine should behave as:

a cognition operating system.

---

# Stage 2 — Intent Behavior

## intent_behavior

Purpose:

interpret user communication intent.

Example:

```yaml
intent_behavior:

  detect:
    - communication_goal
    - audience_type
    - cognition_failure
    - refinement_request

  prevent:
    - literal_prompt_interpretation
```

The runtime should interpret:

cognitive meaning,
NOT only words.

---

# Stage 3 — Workflow Behavior

## workflow_behavior

Purpose:

coordinate workflow activation.

Example:

```yaml
workflow_behavior:

  activate:
    - academic_workflow
    - revision_workflow

  preserve:
    dominant_workflow

  prevent:
    workflow_conflict
```

One workflow must dominate:

runtime behavior.

---

# Stage 4 — Skill Behavior

## skill_behavior

Purpose:

coordinate behavioral cognition systems.

Example:

```yaml
skill_behavior:

  preserve:
    - academic_skill
    - teaching_skill_support

  prevent:
    behavior_drift
```

Skills regulate:

communication personality.

---

# Stage 5 — Planning Behavior

## planning_behavior

Purpose:

coordinate cognition planning execution.

Example:

```yaml
planning_behavior:

  preserve:
    - hierarchy_reasoning
    - audience_adaptation
    - density_control

  prevent:
    direct_template_generation
```

Planning should remain:

cognition-first.

---

# Stage 6 — Schema Behavior

## schema_behavior

Purpose:

coordinate cognition schema systems.

Example:

```yaml
schema_behavior:

  activate:
    - cognition_slide_schema
    - hierarchy_schema
    - mechanism_schema

  preserve:
    schema_consistency
```

Schemas encode:

communication cognition.

---

# Stage 7 — Renderer Behavior

## renderer_behavior

Purpose:

coordinate cognition-aware renderers.

Example:

```yaml
renderer_behavior:

  activate:
    - hierarchy_renderer
    - semantic_renderer
    - mechanism_renderer

  preserve:
    editorial_rendering_behavior

  prevent:
    decorative_rendering
```

Renderers execute:

cognition reasoning.

---

# Stage 8 — Evaluation Behavior

## evaluation_behavior

Purpose:

coordinate rendering validation.

Example:

```yaml
evaluation_behavior:

  validate:
    - hierarchy_clarity
    - mechanism_readability
    - audience_understanding
    - semantic_consistency

  prevent:
    rendering_instability
```

Evaluation measures:

communication effectiveness.

---

# Stage 9 — Refinement Behavior

## refinement_behavior

Purpose:

coordinate iterative refinement.

Example:

```yaml
refinement_behavior:

  preserve:
    presentation_identity

  refine:
    local_cognition_failures

  prevent:
    unnecessary_regeneration
```

Refinement should feel:

collaborative.

---

# Stage 10 — Memory Behavior

## memory_behavior

Purpose:

preserve cognition continuity.

Example:

```yaml
memory_behavior:

  preserve:
    - workflow_identity
    - audience_model
    - visual_atmosphere
    - hierarchy_behavior

  update:
    revision_history
```

Memory stabilizes:

persistent collaboration.

---

# Stage 11 — Stability Behavior

## stability_behavior

Purpose:

preserve runtime continuity.

Example:

```yaml
stability_behavior:

  preserve:
    - communication_continuity
    - editorial_tone
    - presentation_identity

  prevent:
    - visual_drift
    - cognition_fragmentation
```

Stability improves:

human collaboration experience.

---

# Full Orchestration Runtime Example

```yaml
orchestration_identity:
  persistent_cognition_runtime

intent_behavior:

  detect:
    - communication_goal
    - audience_type
    - cognition_failure

  prevent:
    literal_prompt_interpretation

workflow_behavior:

  activate:
    - academic_workflow
    - revision_workflow

  preserve:
    dominant_workflow

skill_behavior:

  preserve:
    - academic_skill

  prevent:
    behavior_drift

planning_behavior:

  preserve:
    - hierarchy_reasoning
    - density_control

  prevent:
    template_generation

schema_behavior:

  activate:
    - cognition_slide_schema
    - hierarchy_schema
    - mechanism_schema

renderer_behavior:

  activate:
    - hierarchy_renderer
    - semantic_renderer
    - mechanism_renderer

  preserve:
    editorial_rendering_behavior

evaluation_behavior:

  validate:
    - hierarchy_clarity
    - mechanism_readability
    - semantic_consistency

refinement_behavior:

  preserve:
    presentation_identity

  refine:
    local_cognition_failures

memory_behavior:

  preserve:
    - audience_model
    - visual_atmosphere
    - hierarchy_behavior

  update:
    revision_history

stability_behavior:

  preserve:
    - communication_continuity
    - editorial_tone

  prevent:
    - cognition_fragmentation
```

---

# Runtime Orchestration Rules

The orchestration engine must:

- coordinate cognition systems
- preserve workflow dominance
- stabilize presentation identity
- regulate refinement behavior
- preserve audience adaptation
- coordinate renderers intelligently
- maintain communication continuity

The runtime should behave as:

a persistent cognition collaborator.

---

# Anti-AI-Slop Runtime Rules

The orchestration engine must actively prevent:

- stateless generation behavior
- workflow fragmentation
- random renderer activation
- presentation identity collapse
- decorative rendering drift
- template-first reasoning
- unnecessary regeneration

The runtime should feel:

human-guided,
persistent,
and cognitively stable.

---

# Final Validation

Before runtime execution:

Check:

- Is workflow coordination stable?
- Is cognition continuity preserved?
- Is audience adaptation maintained?
- Is refinement localized?
- Is rendering subordinate to cognition?
- Is presentation identity stable?
- Does the runtime feel collaborative rather than generative?

If NO:

refine orchestration behavior.

---

# System Identity

This orchestration engine is NOT:

a generation pipeline.

It IS:

a persistent cognition coordination runtime.

---

# Final Philosophy

Orchestration is NOT:
task sequencing.

It IS:

persistent cognition coordination
for collaborative presentation systems.
```
