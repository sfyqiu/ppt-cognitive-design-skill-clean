# Runtime Execution Examples System

## Purpose

This document provides executable cognition runtime examples for the AI-native presentation cognition system.

The objective is NOT:
static documentation examples.

The objective IS:
demonstrating how cognition orchestration behaves during real execution.

Traditional examples show:

- prompts
- outputs
- templates

This system demonstrates:

- cognition reasoning
- workflow dispatch
- renderer coordination
- artifact generation
- refinement behavior
- persistent collaboration continuity

The runtime examples exist to show:

```text
How the cognition runtime actually thinks.
```

---

# Core Runtime Example Philosophy

Examples should NOT demonstrate:

```text
prompt
↓
slides
```

Examples SHOULD demonstrate:

```text
understanding
↓
cognition planning
↓
workflow orchestration
↓
artifact generation
↓
rendering
↓
evaluation
↓
refinement
↓
persistent collaboration
```

The examples should feel:

alive,
adaptive,
and cognition-aware.

---

# Example 1 — Scientific Conference Presentation

## User Request

```text
Help me prepare a scientific conference presentation
about rhizosphere microenvironment regulation
under membrane-controlled irrigation.
```

---

# Runtime Interpretation

```yaml
presentation_type:
  scientific_conference

primary_workflow:
  academic_workflow

secondary_workflow:
  teaching_workflow

communication_goal:
  mechanism_oriented_scientific_explanation

audience:
  researchers_and_graduate_students
```

The runtime detects:

- scientific mechanism emphasis
- moderate cognition density
- evidence hierarchy importance
- restrained editorial rendering

---

# Cognition Plan Generation

Generated artifact:

```text
artifacts/cognition_plan.md
```

Example cognition plan:

```yaml
narrative_strategy:

  opening:
    agricultural_water_nitrogen_problem

  progression:
    rhizosphere_mechanism_explanation

  ending:
    nitrogen_efficiency_improvement

hierarchy_strategy:

  dominant_focus:
    mechanism_chain
```

The cognition plan stabilizes:

communication objectives.

---

# Slide Blueprint Generation

Generated artifact:

```text
artifacts/slide_blueprint.md
```

Example slide blueprint:

```yaml
slide_goal:
  explain_how_microenvironment_stabilization_improves_nitrogen_conversion

slide_type:
  mechanism_explanation

hierarchy_behavior:

  dominant_focus:
    mechanism_chain
```

The blueprint stabilizes:

slide-level cognition execution.

---

# Rendering JSON Generation

Generated artifact:

```text
artifacts/rendering_json_spec.md
```

Example rendering JSON:

```json
{
  "slide_type": "mechanism_explanation",

  "hierarchy": {
    "dominant_focus": "mechanism_chain"
  },

  "mechanism_structure": {
    "trigger": "membrane_controlled_irrigation",
    "outcome": "nitrogen_efficiency_improvement"
  }
}
```

The JSON becomes:

executable cognition structure.

---

# Renderer Coordination

Activated systems:

```yaml
activate:
  - hierarchy_renderer
  - mechanism_renderer
  - semantic_renderer
```

The runtime prioritizes:

- mechanism readability
- restrained spacing
- scientific hierarchy

---

# Rendering Evaluation

Evaluation results:

```yaml
evaluation:

  hierarchy_clarity:
    passed

  mechanism_readability:
    passed

  density_control:
    moderate_pressure
```

The runtime identifies:

minor density overload.

---

# Refinement Patch

Generated artifact:

```text
artifacts/refinement_patch.md
```

Example refinement:

```yaml
modify_behavior:

  reduce:
    - simultaneous_annotations

  increase:
    - whitespace

  strengthen:
    - directional_flow
```

Refinement remains:

localized and stable.

---

# Example 2 — Undergraduate Teaching PPT

## User Request

```text
Design a teaching presentation
explaining how salinity affects crop growth.
```

---

# Runtime Interpretation

```yaml
presentation_type:
  teaching_presentation

primary_workflow:
  teaching_workflow

secondary_workflow:
  interview_workflow

audience:
  first_year_undergraduates

cognition_density:
  low
```

The runtime prioritizes:

- accessibility
- visual explanation
- guided cognition
- low-density layouts

---

# Slide Blueprint Example

```yaml
slide_goal:
  explain_how_salt_reduces_root_water_absorption

slide_type:
  teaching_principle

hierarchy_behavior:

  dominant_focus:
    root_water_relationship_diagram
```

The runtime prioritizes:

visual understanding.

---

# Renderer Coordination

```yaml
activate:
  - hierarchy_renderer
  - adaptive_layout_engine
  - semantic_renderer
```

The runtime increases:

- whitespace
- diagram size
- annotation simplicity

---

# Rendering Behavior

```yaml
rendering_behavior:

  terminology_complexity:
    low

  visual_guidance:
    strong

  simultaneous_information:
    minimized
```

The slide becomes:

cognitively lighter.

---

# Example 3 — Faculty Interview Presentation

## User Request

```text
Help me prepare a faculty interview PPT
positioning myself as:
water-soil engineering + digital agriculture + spatiotemporal information.
```

---

# Runtime Interpretation

```yaml
presentation_type:
  faculty_interview

primary_workflow:
  interview_workflow

secondary_workflow:
  academic_workflow

communication_goal:
  interdisciplinary_positioning
```

The runtime prioritizes:

- strategic identity
- stable professionalism
- restrained academic tone

---

# Cognition Strategy

```yaml
positioning_strategy:

  dominant_identity:
    interdisciplinary_young_faculty

  avoid:
    pure_remote_sensing_positioning
```

The runtime stabilizes:

professional identity consistency.

---

# Rendering Strategy

```yaml
rendering_behavior:

  editorial_tone:
    restrained_professional

  hierarchy_behavior:
    strategic_positioning_first

  density_behavior:
    medium_low
```

The runtime prevents:

- over-academic overload
- decorative startup aesthetics
- identity fragmentation

---

# Example 4 — Iterative Refinement

## User Feedback

```text
"This slide feels too academic.
Undergraduates may not understand."
```

---

# Runtime Interpretation

```yaml
interpreted_problem:
  terminology_overload
  weak_visual_guidance
  excessive_abstraction
```

---

# Failure Detection

```yaml
failures:

  audience_mismatch:
    true

  density_failure:
    true
```

---

# Renderer Coordination

```yaml
activate:
  - adaptive_layout_engine
  - hierarchy_renderer
```

---

# Refinement Patch

```yaml
modify_behavior:

  simplify:
    - terminology
    - simultaneous_concepts

  enlarge:
    - diagrams

  strengthen:
    - visual_annotations
```

The refinement preserves:

- slide purpose
- presentation identity
- semantic consistency

---

# Cognition Diff

Generated artifact:

```text
artifacts/cognition_diff.md
```

Example cognition evolution:

```yaml
before:
  dense_scientific_explanation

after:
  guided_visual_teaching
```

The runtime explains:

how communication improved.

---

# Example 5 — Persistent Collaboration Continuity

## Runtime Memory State

The runtime preserves:

```yaml
persistent_state:

  workflow_identity:
    teaching_workflow

  audience_model:
    undergraduate_students

  semantic_behavior:
    green_agriculture_blue_water

  refinement_history:
    - density_reduction
    - annotation_simplification
```

The runtime behaves as:

a persistent cognition collaborator.

---

# Full Runtime Execution Example

```yaml
user_request:
  teaching_presentation_about_salinity

runtime_interpretation:
  teaching_workflow

cognition_plan:
  generated

slide_blueprint:
  generated

rendering_json:
  generated

adaptive_layout_spec:
  generated

renderer_coordination:
  active

rendering_evaluation:
  completed

rendering_diagnostics:
  generated

refinement_patch:
  localized

cognition_diff:
  tracked

refinement_trace:
  preserved

persistent_collaboration:
  active
```

---

# Runtime Example Rules

Examples must demonstrate:

- cognition reasoning
- workflow orchestration
- renderer coordination
- artifact generation
- evaluation behavior
- refinement continuity
- persistent collaboration

Examples should prioritize:

communication cognition visibility.

---

# Anti-AI-Slop Example Rules

Examples must actively prevent:

- prompt-output simplification
- template-only demonstrations
- stateless workflows
- regeneration-only behavior
- decorative rendering logic
- disconnected refinement examples

Examples should feel:

persistent,
adaptive,
and cognition-aware.

---

# Final Validation

Before runtime example approval:

Check:

- Does the runtime think cognitively?
- Is workflow orchestration visible?
- Are artifacts generated coherently?
- Is rendering subordinate to cognition?
- Is refinement localized?
- Is collaboration continuity preserved?
- Do examples demonstrate persistent cognition behavior?

If NO:

refine runtime examples.

---

# System Identity

These runtime examples are NOT:

presentation demos.

They ARE:

cognition orchestration demonstrations.

---

# Final Philosophy

Runtime execution examples are NOT:
usage samples.

They ARE:

live cognition orchestration demonstrations
for AI-native presentation systems.
